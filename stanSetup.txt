ToDo:
- Convert this script to makefile
- Setup requirements.txt

Instructions on setup up the stan environment

conda create -n stan_env python=3.9 -c conda-forge
conda activate stan_env
conda install pystan -c conda-forge
(install gcc5, pystan 2.19 requires gcc4.9.3 and above)
brew install gcc
(look for ‘gcc-10’, ‘g++-10’)
ls /usr/local/bin | grep gcc
ls /usr/local/bin | grep g++

In python:
import os
os.environ[‘CC’] = ‘gcc-10’
os.environ[‘CXX’] = ‘g++-10’